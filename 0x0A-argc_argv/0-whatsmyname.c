#include <stdio.h>
#include "main.h"

/**
 * main - Print the logo of the program
 * @argc: Count the args
 * @argv: Args
 *
 * Return: Always 0 (Success)
 */

int main(int argc, char *argv[])
{
(void) argc;
printf("%s\n", argv[0]);

return (0);
}
